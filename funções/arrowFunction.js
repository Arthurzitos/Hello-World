function soma (x, y) {
    // codigo
    const resultado = x + y;
    return resultado;
}

// utilizando arrow function

const soma = (x, y) => {
    // codigo
    const resultado = x + y;
    return resultado;
}

console.log(soma(5, 2)); // retorno da função