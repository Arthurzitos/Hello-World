// começando com variaveis
// a palavra reservada "const" não permite com que o valor da varialvel seja alterado depois

const firtsName = "Arthur"; // atribuindo valor na variável

console.log(firtsName); // imprimindo o resultado em tela